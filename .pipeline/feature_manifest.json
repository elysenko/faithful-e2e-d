{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nBuild a minimal single-container FastAPI + SQLite web app that serves a static HTML/JS landing page titled \"FaithfulD Book Notes\" with create / list-newest-first / delete CRUD for book notes.\n\n## Scope\n### Files to modify\n- None substantive (greenfield). `README.md` optionally updated with run instructions.\n\n### Files to create\n- `backend/app/main.py` — FastAPI app: mounts static frontend, defines API routes, health endpoints, DB init on startup.\n- `backend/app/db.py` — SQLite connection helper + schema bootstrap (`notes` table).\n- `backend/app/models.py` — Pydantic request/response schemas (`NoteCreate`, `NoteOut`).\n- `backend/requirements.txt` — `fastapi`, `uvicorn[standard]`, `pydantic`.\n- `backend/static/index.html` — landing page with `<h1>FaithfulD Book Notes</h1>`, add-note form, notes list container.\n- `backend/static/app.js` — fetches/renders notes newest-first, handles add + delete via API, reads `?confirmDelete=<id>` for delete-confirm state.\n- `backend/static/styles.css` — minimal styling.\n- `Dockerfile` — python:3.12-slim base, install deps, copy `backend/`, run uvicorn on `0.0.0.0:8080`.\n- `.dockerignore` — exclude `.git`, `__pycache__`, local `*.db`.\n\n### Dependencies\n- FastAPI, Uvicorn (ASGI server), Pydantic. Python stdlib `sqlite3` (no ORM). No frontend framework — vanilla HTML/JS.\n\n### Integrations\n- None (no third-party APIs or SDKs).\n\n### Assumptions\n- **Stack choice:** FastAPI + SQLite + vanilla static frontend, single container. Chosen for minimal footprint, no build step, and clean Docker deploy via Colossus. No stack was pre-declared.\n- **No auth:** Spec lists \"No auth, no user accounts\" as an explicit non-goal; the auth baseline is overridden by the authoritative spec. No login, guards, or admin.\n- **Persistence path:** SQLite DB at `/data/faithfuld.db` (env `DB_PATH`, default `./faithfuld.db` locally) so it can map to a persistent volume; startup creates the dir/table if missing.\n- **Port:** App listens on `8080` (common Colossus convention); adjustable via `PORT` env.\n- **Serving model:** FastAPI serves both the JSON API (`/api/*`) and the static SPA from the same origin — no CORS needed.\n- **Note fields:** `id` (int PK), `title` (text, required, non-empty), `body` (text, required, short), `created_at` (ISO timestamp). Newest-first = `ORDER BY id DESC`.\n- **Routing:** Single page; the only non-trivial UI state (delete confirmation) is URL-addressable via `?confirmDelete=<id>` restored on load. Transient form input is exempt.\n\n## Implementation Plan\n\n### Step 1: Database layer\n**File:** `backend/app/db.py`\n**Change:** `get_conn()` returning a `sqlite3.Connection` (row factory = `sqlite3.Row`). `init_db()` reads `DB_PATH`, ensures parent dir exists, and runs `CREATE TABLE IF NOT EXISTS notes (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, body TEXT NOT NULL, created_at TEXT NOT NULL)`.\n\n### Step 2: Schemas\n**File:** `backend/app/models.py`\n**Change:** `NoteCreate(title: str, body: str)` with validators enforcing non-empty, trimmed, reasonable max lengths (title ≤200, body ≤1000). `NoteOut(id, title, body, created_at)`.\n\n### Step 3: API + app wiring\n**File:** `backend/app/main.py`\n**Change:** Create `FastAPI()` app. On startup call `init_db()`. Routes:\n- `GET /api/health` → `{\"status\":\"ok\"}`.\n- `GET /api/health/deep` → runs `SELECT 1` against DB, returns ok/500.\n- `GET /api/notes` → all notes `ORDER BY id DESC` → `list[NoteOut]`.\n- `POST /api/notes` → insert `NoteCreate`, set `created_at` = UTC ISO now, return created `NoteOut` (201).\n- `DELETE /api/notes/{note_id}` → delete row; 204 on success, 404 if absent.\nMount static: `app.mount(\"/\", StaticFiles(directory=\"backend/static\", html=True), name=\"static\")` (mounted last so `/api/*` wins).\n\n### Step 4: Landing page\n**File:** `backend/static/index.html`\n**Change:** `<h1>FaithfulD Book Notes</h1>` (ensures literal \"FaithfulD\" renders). Form with `#title` input, `#body` textarea, submit button. `<ul id=\"notes\">` list container. Links `styles.css` and `app.js`.\n\n### Step 5: Frontend logic\n**File:** `backend/static/app.js`\n**Change:** On load: `loadNotes()` → `GET /api/notes`, render each as list item with title, body, timestamp, and a Delete button. Form submit → `POST /api/notes` then reload list and clear form. Delete button sets `?confirmDelete=<id>`; on load if that param is present, show inline confirm → on confirm `DELETE /api/notes/{id}`, then clear param and reload. Escape user text via `textContent` to avoid injection.\n\n### Step 6: Styling\n**File:** `backend/static/styles.css`\n**Change:** Minimal readable layout — centered container, form spacing, note cards.\n\n### Step 7: Containerization\n**File:** `Dockerfile`\n**Change:** `FROM python:3.12-slim`; set workdir; copy `requirements.txt` and `pip install`; copy `backend/`; `ENV DB_PATH=/data/faithfuld.db PORT=8080`; `EXPOSE 8080`; `CMD [\"sh\",\"-c\",\"uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT}\"]`. Add `.dockerignore`.\n\n## Testing Strategy\n- **Health:** `curl /api/health` → 200 ok; `/api/health/deep` → 200 with DB reachable.\n- **Create:** `POST /api/notes` with title+body → 201 and returned object has id + created_at.\n- **List order:** create two notes; `GET /api/notes` returns the second one first (id DESC).\n- **Persist:** restart container (same volume) → notes still returned.\n- **Delete:** `DELETE /api/notes/{id}` → 204; subsequent GET omits it; deleting unknown id → 404.\n- **UI:** load page, confirm \"FaithfulD Book Notes\" renders in `<h1>`; add a note via form → appears at top; delete via button + confirm → disappears.\n- **Validation:** empty title or body → 422.\n\n## Risks\n- Container filesystem is ephemeral — DB must sit on a mounted/persistent path (`/data`) or notes are lost on redeploy; documented via `DB_PATH`.\n- SQLite single-writer is fine for this low-traffic, no-concurrency-critical app; not suited to horizontal scaling (out of scope).\n- Static mount ordering must keep `/api/*` routes resolving before the catch-all static mount.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {}
}