# Atlas Stack Status
Generated by scaffolder.

## Verdict: GREENFIELD

## Plan
- Runtime: FastAPI (Python 3.12) + SQLite (stdlib sqlite3)
- Frontend: Vanilla HTML/JS/CSS served as static files by FastAPI (`backend/static/`)
- Port: 8080 (env `PORT`)
- DB: SQLite at `/data/faithfuld.db` (env `DB_PATH`)
- Container: Single-image Docker build (`python:3.12-slim` base)

## Note on scaffolded templates
The NestJS template (`backend/`) and Angular template (`web/`) were copied as structural scaffolding.
The **coder agent must REPLACE them entirely** with the FastAPI + vanilla HTML implementation described in the plan.
Do not build on the NestJS/Angular template code.
